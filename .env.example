# Example env file for docker-compose (optional)
# You can create a .env file at the project root and set variables used by docker-compose if desired
# For example:
# JUPYTER_TOKEN=yourtoken

# Use official Apache Spark image (Java 21 + Python 3)
# See https://hub.docker.com/_/spark for available tags
# Examples:
# Examples (choose one and copy to .env without '#'):
# SPARK_IMAGE=spark:3.5.1-scala2.12-java17-python3
# SPARK_IMAGE=spark:3.5.0-scala2.12-java17-python3
# If you really need Java 21, pick an available tag from Docker Hub: https://hub.docker.com/_/spark
# Default fallback (used if .env is absent): 3.5.1 + Java 17 + Python 3
